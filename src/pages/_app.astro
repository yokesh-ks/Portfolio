---
import "../styles/globals.css";
import { useRouter } from 'next/router'

import Main from "../components/layout/main.astro";

const {Component, pageProps} = Astro.props

const router = useRouter()

---

<>

<Main />
  <!-- <GlobalStyle /> -->
  <Component {...pageProps} key={router.route}/>
</>
