---
// import Head from "next/head";
// import {Montserrat} from '@next/font/google'

const title = "Yokesh Portfolio"
const description=  "Generated by create next app"
const keywords =  "Yokesh, Portfolio, Full Stack, Developer"

// const montserrat = Montserrat({
//   subsets: ["latin"],
//   variable: "--font-mont"
// })

export interface Props {
  children?: astro.Node;
  titleNew: String
}

const { titleNew, children } = Astro.props
console.log("Hello")
console.log(titleNew, children)
---

<head>
  <title>{title}</title>
  <meta name="description" content={description} />
  <meta name="keywords" content={keywords} />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" href="/favicon.ico" />
</head>

<body>
  <main class="font-mont bg-light">
    {children}
  </main>

</body>