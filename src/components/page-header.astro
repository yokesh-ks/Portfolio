---
import { cn } from "@/lib/utils";

import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"div"> {
  heading: string;
  text?: string;
  className?: string;
}
const { heading, text, className, ...attrs } = Astro.props;
---

<div class={cn("space-y-4", className)} {...attrs}>
  <h1 class="inline-block font-heading text-4xl text-gradient_indigo-purple">
    {heading}
  </h1>
  {text && <p class="text-lg text-muted-foreground">{text}</p>}
</div>
<hr class="my-4" />
